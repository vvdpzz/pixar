<h1><%= current_user.credit %></h1>
<%= link_to 'Recharge', new_recharge_url %>
<table>
	<tbody>
		<% @orders.each do |order| %>
			<tr>
				<td><%= order.id %></td>
				<td><%= order.credit %></td>
				<td><%= link_to 'pay', recharge_url(order) if order.trade_status == 0 %></td>
			</tr>
		<% end %>
	</tbody>
</table>
			